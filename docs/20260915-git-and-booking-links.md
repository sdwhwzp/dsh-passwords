# 2026-09-15 兼容修复

配对本机工作区的右侧 Git 只读查询通过账号授权和配对所有权检查后转发给原有助手。状态、差异、分支、日志只覆盖已授权目录；仓库修改仍由 Agent 执行。不能向其他工作树路径转发。普通服务器目录继续由原有面板处理。断线、缺少 Git、Shell 未启用不能伪装成不是 Git 仓库。

Windows 配对 Git 查询通过 PowerShell 的调用运算符执行带引号的程序路径，使用 UTF-8 输出并保留 Git 退出码。兼容性检查覆盖命令生成、路径空格、单引号和中文参数；Windows 桌面安装包的实际运行仍需 Windows 环境验收。
